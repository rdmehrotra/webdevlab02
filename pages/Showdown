import streamlit as st
import random as r
import requests
st.header('Showdown')
st.write('This is a showdown between two characters of your choice, we also give a tell you each character's powers.')
url = 'https://akabab.github.io/superhero-api/api/all.json'
respouse = requests.get(url)
hero_list = []
for hero in heroes:
  hero_list.append(hero['name'])
st.selectbox('Pick hero 1', hero_list, index=0)
st.selectbox('Pick hero 2', hero_list, index=1)
st.write('You may also hit the random bottom if you cannot choose')
